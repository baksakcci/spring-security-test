spring:
  application:
    name: spring-security-test
  datasource:
    url: jdbc:h2:~/security-test;DB_CLOSE_DELAY=-1;MODE=MySQL;
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
  security:
    oauth2:
      client:
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
        registration:
          kakao:
            client-id: c8c9d6bab40971123f44a3e29677e1e7
            client-secret: z3UdQynLXo1Yz2KjRrRiRgB8rC9JWRoq
            client-authentication-method: client_secret_post
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            client-name: kakao
            scope:
              - profile_nickname
              - account_email

logging:
  level:
    org.springframework.security.* : TRACE

jwt:
  secret_key: thisissangccispringsecuritytestiamveryhappyyeahyeah

  access:
    expiration: 3600000 # 1시간(60분) (1000L(ms -> s) * 60L(s -> m) * 60L(m -> h))
    header: Authorization
  refresh:
    expiration: 1209600033 # 2주
    cookie-name: refreshToken